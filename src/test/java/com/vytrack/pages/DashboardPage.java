package com.vytrack.pages;

public class DashboardPage extends BasePage { //to use methods in BasePage , I created. because Base page is abstract
    //no need to explicitly write constructor
    //because it will use its parents constructor

}
